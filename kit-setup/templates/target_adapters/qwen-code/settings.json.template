{
  "contextFileName": "AGENTS.md",
  "modelProviders": {
    "openai": [
      {
        "id": "{{RESOLVED_MODEL}}",
        "name": "{{RESOLVED_MODEL}}",
        "baseUrl": "{{PROVIDER_BASE_URL}}"
      }
    ]
  },
  "model": { "name": "{{RESOLVED_MODEL}}" },
  "permissions": {
    "allow": {{PERMISSIONS_ALLOW_JSON}},
    "deny": {{PERMISSIONS_DENY_JSON}}
  },
  "mcpServers": {{MCP_SERVERS_JSON}}
}
